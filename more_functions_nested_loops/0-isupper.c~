#include "main.h"

/**
 * _isupper - check if char is in uppercase
 * Return: 1 if yes, 0 if no
 * @c: char param
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return 1;
	}
	if (c >= 97 && c <= 122)
	{
		return 0;
	}
}
