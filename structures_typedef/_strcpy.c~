#include <string.h>

/**
 * _strcpy - copy a string
 * @dest: string copied
 * @src: string to copy
 * Return: String copy
 */

char *_strcpy(char *dest, char *src)
{
	int i;

	for (i = 0; i <= strlen(src); i++)
	{
		dest[i] = src[i];
	}

	return (dest);
}
